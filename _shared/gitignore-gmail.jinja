{% if bot_gmail_effective %}
# Gmail OAuth grant (never commit the inbox): the credentials file downloaded
# from Google Cloud Console and the token the first consent writes — the
# same never-commit rule as the CTF challenges' flag* files
/credentials.json
/token.json
{%- endif %}