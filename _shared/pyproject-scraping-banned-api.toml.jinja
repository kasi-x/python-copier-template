{% if scraping_effective %}[tool.ruff.lint.flake8-tidy-imports.banned-api]
"requests.get".msg = "Use {{ import_pkg }}.fetcher.fetch_text instead (robots.txt + rate limit + cache)."
"requests.post".msg = "Use {{ import_pkg }}.fetcher.fetch_text instead (robots.txt + rate limit + cache)."
"requests.put".msg = "Use {{ import_pkg }}.fetcher.fetch_text instead (robots.txt + rate limit + cache)."
"requests.delete".msg = "Use {{ import_pkg }}.fetcher.fetch_text instead (robots.txt + rate limit + cache)."
"requests.patch".msg = "Use {{ import_pkg }}.fetcher.fetch_text instead (robots.txt + rate limit + cache)."
"requests.head".msg = "Use {{ import_pkg }}.fetcher.fetch_text instead (robots.txt + rate limit + cache)."
"requests.options".msg = "Use {{ import_pkg }}.fetcher.fetch_text instead (robots.txt + rate limit + cache)."
"requests.request".msg = "Use {{ import_pkg }}.fetcher.fetch_text instead (robots.txt + rate limit + cache)."
"httpx.get".msg = "Use {{ import_pkg }}.fetcher.fetch_text instead (robots.txt + rate limit + cache)."
"httpx.post".msg = "Use {{ import_pkg }}.fetcher.fetch_text instead (robots.txt + rate limit + cache)."
"httpx.put".msg = "Use {{ import_pkg }}.fetcher.fetch_text instead (robots.txt + rate limit + cache)."
"httpx.delete".msg = "Use {{ import_pkg }}.fetcher.fetch_text instead (robots.txt + rate limit + cache)."
"httpx.patch".msg = "Use {{ import_pkg }}.fetcher.fetch_text instead (robots.txt + rate limit + cache)."
"httpx.head".msg = "Use {{ import_pkg }}.fetcher.fetch_text instead (robots.txt + rate limit + cache)."
"httpx.options".msg = "Use {{ import_pkg }}.fetcher.fetch_text instead (robots.txt + rate limit + cache)."
"httpx.request".msg = "Use {{ import_pkg }}.fetcher.fetch_text instead (robots.txt + rate limit + cache)."
"urllib.request.urlopen".msg = "Use {{ import_pkg }}.fetcher.fetch_text instead (robots.txt + rate limit + cache)."
{% endif %}