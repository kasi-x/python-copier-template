# Shared "Project Details" answers for batch/smoke.jsonl.
#
# Each request in the batch merges its own inline `answers` on top of this
# file, so a line only has to state what makes it different (project_type,
# layer opt-ins, ...). Exactly the shared set in tools/answers.py that the
# pytest fixtures start from -- tests/test_answer_fixtures.py compares the two,
# so the batch and the fast paths exercise the same questionnaire entries.
# repo_name / distribution_name are not listed: copier derives both from
# package_name.
package_name: smoke_example
description: An example project
git_platform: github.com
github_org: kasi-x
author_name: kasi-x
author_email: kashimiya.exe@gmail.com
