# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]

# C extensions
*.so

# Distribution / packaging
.Python
env/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
*.egg-info/
.installed.cfg
*.egg
**/_version.py

# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.task/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
cov.xml
.pytest_cache/
.mypy_cache/

# Translations
*.mo
*.pot

# Django stuff:
*.log

# Sphinx documentation
docs/_build/
docs/_api

# Zensical (mkdocs) output
/site/

# Quarto paper / slides output
*.quarto_ipynb
paper/paper.aux
paper/arxiv.sty
paper/orcidlink.sty
paper/paper.pdf
paper/paper.tex
paper/paper.html
paper/*.bbl
paper/*.log
paper/paper_files/**
slides/*_slides.html
slides/slides_files/*

# DS project data
notebooks/*
!notebooks/.gitkeep
data/raw/*
!data/raw/.gitkeep
data/interim/*
!data/interim/.gitkeep
data/processed/*
!data/processed/.gitkeep
data/external/*
!data/external/.gitkeep
models/*
!models/.gitkeep
reports/figures/*
!reports/figures/.gitkeep
outputs/*
!outputs/.gitkeep
# Kaggle competition data
src/input/*
!src/input/.gitkeep
src/output/*
!src/output/.gitkeep
src/logs/*
!src/logs/.gitkeep
src/models/*
!src/models/.gitkeep
src/features/*
!src/features/.gitkeep
src/data/*
!src/data/.gitkeep
src/notebook/*
!src/notebook/.gitkeep

# PyBuilder
target/

# likely venv names
.venv*
venv*

# pixi environments
.pixi/*
!.pixi/config.toml

# further build artifacts
lockfiles/

# ruff cache
.ruff_cache/

# Local AI/editor tool caches
.codegraph/
.commandcode/
.omo/
.pi/

# Local environment variables (see .env.example)
.env
.envs/
*.pem

# ROS 2 / colcon workspace artifacts (union of the conditional ros2 branch —
# generated non-ROS projects don't get these)
install/
log/
# MicroPython type stubs — installed into typings/, not committed
typings/
# MicroPython source tree cloned by the freeze task, and its output
# (dist/ is already ignored above under Distribution/packaging)
build/micropython-src/

# Local test database (web API tests fall back to SQLite when no DATABASE_URL)
.test.db

# CTF challenge binaries / downloaded assets (union of the conditional ctf
# branch — generated non-CTF projects don't get these)
challenges/**/vuln
challenges/**/flag*

# Polite-fetcher cache and Playwright browser profiles (union of the
# conditional scraping branch — generated non-scraping projects don't get these)
.cache/fetcher/
.cache/ms-playwright/
