{% import "_tasks.jinja" as _t with context -%}# Agent Guide for `{{ repo_name }}`

This file is for AI coding agents working in this repository. It states how
to run the checks and where edits belong. The human-facing contribution
guide is [.github/CONTRIBUTING.md](.github/CONTRIBUTING.md).
{% if kaggle %}
> Kaggle competition project: shared helpers live in `src/utils/`
> (`from utils.config import ...`); competition data goes in `src/input/`
> and predictions are written to `src/output/`. Do not commit data,
> predictions, or logs — they are git-ignored.
{% elif oj_code %}
> Code-submission workspace ({{ oj_kind }}): write solutions as single files
> (e.g. `main.py`). Per-problem folders and `test/` sample cases are created
> by the judge tooling at runtime — do not commit downloaded cases
> (oj / acc for AtCoder, oj for yukicoder and Codeforces, aoj-cli for AOJ,
> submit.py + `.kattisrc` for Kattis, the judge's editor for LeetCode/other).
{% if oj_allow_ai %}
> The judge's rules permit AI assistance, so agents may help build and
> debug here. Rules override this statement: re-check them before submitting.
{% else %}
> Contest rules govern AI use, and they vary per contest — some ban
> AI-generated answers outright. Do not produce submissions before the
> current rules for {{ oj_kind }} have been checked and allow it.
{% endif %}
{% elif oj_ctf %}
> CTF participant workspace: per-problem starters live in
> `challenges/<category>/<problem>/solve.py`. Run one directly
> (`python challenges/pwn/example/solve.py`), then adapt the offset /
> target. Never commit binaries (`vuln`) or flags — they are git-ignored.
{% elif web_api %}
> Web API service: the application lives in the top-level `app/` package
> (no `<pkg>` library). Run it with `uvicorn app.main:app --reload`.
{% if data_science_layout %}
>
> The data-science layer coexists: `src/` holds the analysis pipeline
> (`data/`, `features/`, `models/`, `visualization/`) and `notebooks/` /
> `reports/` the exploration artifacts. Keep heavy analysis out of `app/`;
> routers expose results computed by `src/` code. Do not commit data,
> models or reports — they are git-ignored.
{% endif %}
{% elif data_science_layout %}
> Data-science project: `src/` holds the importable package (`{{ pkg_dir }}/`)
> beside the analysis pipeline stubs (`data/`, `features/`, `models/`,
> `visualization/`, each a `.gitkeep` stub). Promote notebook cells into
> tested modules there; the stubs are linted like any other `src/` code,
> so new third-party imports need a `[project] dependencies` entry first.
> Rename or extend the stubs via the `src_dirs` answer
> (`.copier-answers.yml`).
{% endif %}

## Commands
The `{{ task_runner_effective }}` task runner drives the common commands:

```sh{# The rows below are the `_tasks.jinja` model, not a hand-written table:
   a row is printed only when the task exists in this render. Presentation
   (comments on the `task` runner, the fix comment on `just`) is kept as is. #}
{% for task_name in _t.agent_cmd_specs if task_name in (_t.tasks | map(attribute='name')) and (task_runner_effective == 'task' or task_name != 'type-check') %}{{ _t.agent_prefix }} {% if task_name in _t.agent_commented %}{{ task_name }}{{ (15 - (task_name | length)) * ' ' }}# {{ _t.agent_cmd_specs[task_name] }}{% else %}{{ task_name }}{% endif %}
{% endfor %}```

{% if 'fix' in (_t.tasks | map(attribute='name')) %}Run `{{ task_runner_effective }} fix` before committing to apply formatting
and lint fixes, then `{{ task_runner_effective }} check` (or `lint` + `test`
{% if 'type-check' in (_t.tasks | map(attribute='name')) %}+ `type-check` {% endif %}individually) before finishing a change.
{% else %}Run `{{ task_runner_effective }} check` (or its steps — `lint`, `build`, `test` —
individually) before finishing a change.
{% endif %}
The repo-hygiene checks (secrets, workflow linting, YAML validity,
conventional commit messages) run in CI, not as local hooks — the lint and
fix tasks work anywhere, including outside a git repository.
{% if 'type-check' in (_t.tasks | map(attribute='name')) %}
Type checking uses basedpyright plus `{{ type_checker }}`; `deptry`,
`vulture`, and `typos` also run as part of type-check.
{% else %}
Type checking is minimal at `strictness: {{ strictness }}` — pytest and
ruff are the checks that matter.
{% endif %}
{% if 'docs' in (_t.tasks | map(attribute='name')) %}
Build the docs with `{{ task_runner_effective }} docs`.
{% endif %}
## Where to edit

- Application/package code: `{{ pkg_dir }}/`
- Tests: `tests/`
- Docs: `README.md`{% if docs %} and `docs/`{% endif %}

Keep 100% coverage where it exists; do not lower it.
{% if scraping_effective %}
## Web-fetching rules (Good-future charter)

- Every web fetch goes through `{{ pkg_dir }}/fetcher.py`
{% if scraping_scrapy_effective %}  (or the `{{ pkg_dir }}/spider.py` spider)
{% endif %}{% if scraping_memorious_effective %}  (or the `{{ pkg_dir }}/crawler.py` crawler)
{% endif %}{% if scraping_playwright_effective %}  (or `{{ pkg_dir }}/browser_fetch.py` for JS-rendered pages)
{% endif %}  — never call `requests.get` / `httpx.get` / `urlopen` directly
  (ruff `banned-api` fails the build).
- Read [CHARTER.md](CHARTER.md) before touching fetch code: robots.txt,
  1 req/s per host, disk cache, contactable User-Agent.
- Never generate CAPTCHA-solving helpers. If a site blocks bots, stop and
  ask the maintainer — do not bypass the protection.
{% endif %}
{% if data_science_layout %}
Data, notebooks, and reports (`data/`, `notebooks/`, `models/`,
`reports/`) are analysis artifacts — keep generated outputs out of git.
{% endif %}
## Commits and CI

- Use [Conventional Commits](https://www.conventionalcommits.org/)
  (`feat:`, `fix:`, ...); the repository's hygiene CI enforces it on commit
  messages and the PR title.
- CI runs lint, type-check, and tests on every push
{% if docs %} plus a docs build{% endif %}; keep all of them green.
{# The ethics appendix: sections live in _shared/ethics/ (one file per rule,
   registered in its REGISTRY.yml). A section ships only where its registry
   audience matches this render, which is what the gates below encode. The
   include chain stays on one line: each section file carries exactly one
   trailing newline (and starts with a jinja comment, whose newline is the
   blank line between sections), so the rendered guide ends with a single
   newline for every gate combination. #}
## Field rules (ethics appendix)

Field rules the generator matched to this project kind (crypto choices,
dependency license drift, data-collection copyright, PKI trust-chain
hygiene, LLM/MCP tool security, model-evaluation fairness), plus the
rules your `domain_traits`
answer selected (personal data, biometric identification, medical device
software), and the EU Cyber Resilience Act's duties where the project is
sold or packaged as a product. Each section opens with a scope blockquote: if its triggers do
not apply to the code you are editing, skip that section. Sections are
written in Japanese.
{% include "_shared/ethics/baseline/license-drift.md.jinja" %}{% if project_type in ['micropython', 'cli'] or web_api %}{% include "_shared/ethics/baseline/pki-chain.md.jinja" %}{% endif %}{% if 'personal-data' in domain_traits or 'face-recognition' in domain_traits or 'medtech' in domain_traits %}{% include "_shared/ethics/baseline/personal-data.md.jinja" %}{% endif %}{% if project_type in ['library', 'cli'] or web_api %}{% include "_shared/ethics/baseline/pqc-fips.md.jinja" %}{% endif %}{% if project_type == 'cli' or data_science_layout %}{% include "_shared/ethics/baseline/copyright-ai.md.jinja" %}{% endif %}{% if mcp_effective %}{% include "_shared/ethics/domain/llm-appsec.md.jinja" %}{% endif %}{% if ds_stack %}{% include "_shared/ethics/domain/ml-bias.md.jinja" %}{% endif %}{% if 'face-recognition' in domain_traits %}{% include "_shared/ethics/domain/face-recognition.md.jinja" %}{% endif %}{% if 'medtech' in domain_traits %}{% include "_shared/ethics/sector/samd-regulatory.md.jinja" %}{% endif %}{% if 'commercial' in distribution %}{% include "_shared/ethics/region/eu-cra.md.jinja" %}{% endif -%}
