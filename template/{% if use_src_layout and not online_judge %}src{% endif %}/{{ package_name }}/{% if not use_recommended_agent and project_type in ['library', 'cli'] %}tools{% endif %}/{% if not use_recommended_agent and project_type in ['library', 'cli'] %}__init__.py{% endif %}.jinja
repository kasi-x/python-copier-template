"""Typed tools the {{ repo_name }} agent can call.

Each module in this package exposes one or more callables that the agent in
`{{ package_name }}.agent` registers with ``@agent.tool``. Keeping tools in
their own package (rather than inline in the agent module) makes them unit-
testable and reusable across agents.
"""
