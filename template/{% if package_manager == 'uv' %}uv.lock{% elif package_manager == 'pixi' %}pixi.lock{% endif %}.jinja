{% if package_manager == "uv" %}version = 1
revision = 3
requires-python = ">=3.11"
{% elif package_manager == "pixi" %}version: 6
environments: {}
packages: []
{% else %}# poetry.lock is generated by `poetry lock`. Commit it after the first sync.
{% endif %}
