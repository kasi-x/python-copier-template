"""Workspace smoke test for a code-submission (online judge) repository.

The repo starts empty on purpose — per-problem folders and sample cases are
created by oj/atcoder-cli, not by the template. This trivial test keeps
`pytest` (and therefore `task check` / CI) green until the first real
test file is added.
"""


def test_workspace_ready() -> None:
    """The generated workspace is import-clean and ready for solutions."""
    assert True
