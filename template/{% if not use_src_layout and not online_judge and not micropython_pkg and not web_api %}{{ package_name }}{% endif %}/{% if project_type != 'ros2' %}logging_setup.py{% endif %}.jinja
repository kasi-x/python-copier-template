{# Logging setup body is shared across project types (see
   _shared/logging_setup.py.jinja); this file only picks the location the
   owning <pkg> expects. #}
{% include "_shared/logging_setup.py.jinja" %}